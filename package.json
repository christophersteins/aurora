{
  "name": "aurora",
  "version": "1.0.0",
  "description": "Modern, fast and secure web app - Project Aurora",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "lint": "npm run lint --workspaces"
  },
  "keywords": [
    "social",
    "video",
    "chat",
    "geolocation"
  ],
  "author": "christophersteins",
  "license": "MIT",
  "dependencies": {
    "zustand": "^5.0.8"
  }
}
